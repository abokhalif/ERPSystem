namespace Common
{
	// any data type in C# defines in namespace level
	// if u want to access any defined data type inside this namespace 
	struct TypeA
	{
		internal int q;// interanal => access inside the assembly file (project file)
	}
}